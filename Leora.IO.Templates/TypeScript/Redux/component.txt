require("./{{ entityNameSnakeCase }}.component.css");

import { CanActivate, ChangeDetectionStrategy, Component } from "../core";
import { {{ entityNamePascalCase }}ActionCreator } from "./{{ entityNameSnakeCase }}.actions";

@Component({
    templateUrl: "wwwroot/{{ entityNameSnakeCase }}/{{ entityNameSnakeCase }}.component.html",
	styleUrls: ["wwwroot/{{ entityNameSnakeCase }}/{{ entityNameSnakeCase }}.component.css"],
    selector: "{{ entityNameSnakeCase }}",
    viewProviders: ["{{ entityNameCamelCase }}ActionCreator"],
	changeDetection: ChangeDetectionStrategy.OnPush
})
export class {{ entityNamePascalCase }}Component {
    constructor(private {{ entityNameCamelCase }}ActionCreator: {{ entityNamePascalCase }}ActionCreator) { }
  
}
