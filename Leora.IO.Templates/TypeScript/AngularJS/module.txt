module app.{{ moduleNameCamelCase }} {

    angular.module("app.{{ moduleNameCamelCase }}", [
            "app.configuration",
            "app.common",
            "app.core",
            "app.session",
            "ngRoute"
        ])
        .config(["$routeProvider", config])
        .run([run]);

    function config($routeProvider) {

        $routeProvider.when("/{{ moduleNameCamelCase }}",
            {
                templateUrl: "app/{{ moduleNameCamelCase }}/templates/index.html",
                resolve: {
                    routeData: []
                },
                authorizationRequired: false,
                caseInsensitiveMatch: true

            });

        //$routeProvider.otherwise("/");
    }

    function run() {
        
    }

	export class {{ moduleName }}Controller {

        constructor() {

        }

    }

	angular.module("app.{{ moduleNameCamelCase }}").controller("{{ moduleNameCamelCase }}Controller", [{{ moduleName }}Controller]);

} 