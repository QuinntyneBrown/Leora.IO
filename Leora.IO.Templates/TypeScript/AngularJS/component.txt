module app.{{ moduleNameCamelCase }} {

    "use strict";

    class {{ componentName }}  implements ng.IDirective {

        constructor() {
            this.link = this._link.bind(this);
        }

		static instance(): ng.IDirective {
            return new {{ componentName }}();
        }

		// ng.I{{ componentName }}Scope

		link: (scope: any, element: ng.IAugmentedJQuery, attrs: ng.IAttributes) => void;

        _link(scope: any, element: ng.IAugmentedJQuery, attributes: ng.IAttributes) {

            scope.{{ componentNameCamelCase }} = this;

            scope.{{ componentNameCamelCase }}.element = element[0];

            scope.{{ componentNameCamelCase }}.attributes = attributes;

        }

        public replace: boolean = true;

        public scope: any = {};

        public templateUrl: string = "/app/{{ moduleNameCamelCase }}/components/{{ componentNameCamelCase }}/{{ componentNameCamelCase }}.html";

        public restrict: string = "E";   

		
        private _attributes: ng.IAttributes;

        public set attributes(attributes: ng.IAttributes) {

            this._attributes = attributes;
        }

        public get attributes() {

            return this._attributes;
        }


        private _element: HTMLElement;

        public set element(element: HTMLElement) {

            this._element = element;
        }

        public get element() {

            return this._element;
        }		     

    }

    angular.module("app.{{ moduleNameCamelCase }}").directive("{{ componentNameCamelCase }}",[{{ componentName }}.instance]);

}
