describe("{{ componentName }} Tests", function () {

    var element = null;
    var scope = null;
    var rootScope = null;

    beforeEach(function () {
        module("{{ moduleNameCamelCase }}");
    });

    beforeEach(inject(function ($rootScope, $compile, $templateCache, getStringFromUrl) {
        $templateCache.put("/app/{{ moduleNameCamelCase }}/components/{{ componentNameCamelCase }}/{{ componentNameCamelCase }}.html", getStringFromUrl("/app/{{ moduleNameCamelCase }}/components/{{ componentNameCamelCase }}/{{ componentNameCamelCase }}.html"));
        element = angular.element("<{{ componentNameSnakeCase }}></{{ componentNameSnakeCase }}>");
        scope = $rootScope.$new();
        rootScope = $rootScope;
        $compile(element)(scope);
        scope.$digest();
    }));

    it("should be defined", function () {
        expect(element).toBeDefined();
    });

});