<cs-{{ entityNameSnakeCase }}-list-item *ngFor="let {{ entityNameCamelCase }} of {{ entityNameCamelCase }}s" [{{ entityNameCamelCase }}]="{{ entityNameCamelCase }}" (select)="select.emit($event)" (delete)="delete.emit($event)"></cs-{{ entityNameSnakeCase }}-list-item>