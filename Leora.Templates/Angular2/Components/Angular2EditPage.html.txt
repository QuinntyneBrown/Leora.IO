<div class="{{ entityNameSnakeCase }}-edit-page">
    <one-column-layout>
        <{{ entityNameSnakeCase }}-edit-form (onSubmit)="onSubmit($event)"></{{ entityNameSnakeCase }}-edit-form>
    </one-column-layout>    
</div>
