import { Injectable } from "@angular/core";
import { HttpClient } from "@angular/common/http";
import { {{ entityNamePascalCase }} } from "./{{ entityNameSnakeCase }}.model";
import { Observable } from "rxjs/Observable";

@Injectable()
export class {{ entityNamePascalCase }}sService {
    constructor(
        private _httpClient: HttpClient)
    { }

    public addOrUpdate(options: { {{ entityNameCamelCase }}: {{ entityNamePascalCase }}}) {
        return this._httpClient
            .post(`${this._baseUrl}/api/{{ entityNameCamelCase }}s/add`, options);
    }

    public get(): Observable<{ {{ entityNameCamelCase }}s: Array<{{ entityNamePascalCase }}> }> {
        return this._httpClient
            .get<{ {{ entityNameCamelCase }}s: Array<{{ entityNamePascalCase }}> }>(`${this._baseUrl}/api/{{ entityNameCamelCase }}s/get`);
    }

    public getById(options: { id: number }): Observable<{ {{ entityNameCamelCase }}:{{ entityNamePascalCase }}}> {
        return this._httpClient
            .get<{{{ entityNameCamelCase }}: {{ entityNamePascalCase }}}>(`${this._baseUrl}/api/{{ entityNameCamelCase }}s/getById?id=${options.id}`);
    }

    public remove(options: { {{ entityNameCamelCase }}: {{ entityNamePascalCase }}}) {
        return this._httpClient
            .delete(`${this._baseUrl}/api/{{ entityNameCamelCase }}s/remove?id=${options.{{ entityNameCamelCase }}.id}&correlationId=${options.correlationId}`);
    }

    public get _baseUrl() { return ""; }
}
