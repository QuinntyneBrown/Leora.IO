export class {{ entityNamePascalCase }} { 

    public id:any;
    
    public name:string;

    public static fromJSON(data: { name:string }): {{ entityNamePascalCase }} {

        let {{ entityNameCamelCase }} = new {{ entityNamePascalCase }}();

        {{ entityNameCamelCase }}.name = data.name;

        return {{ entityNameCamelCase }};
    }
}