import { Action } from "@ngrx/store";
import { {{ entityNameAllCaps }} } from "../actions";
import { initialState } from "../initial-state";

export const userLoggedOut = (state = initialState, action: Action) => {
    switch (action.type) {

        case {{ entityNameAllCaps }}:
            break;

        default:
            return state;
    }
}
