using {{ rootNamespacename }}.Data;
using {{ rootNamespacename }}.Services;
using Microsoft.Practices.Unity;

namespace {{ namespacename }}
{    
    public class {{ entityNamePascalCase }}ContentModel: I{{ entityNamePascalCase }}ContentModel
    {
        [InjectionConstructor]
        public {{ entityNamePascalCase }}ContentModel(IUow uow, ICacheProvider cacheProvider)
        :this(uow,cacheProvider.GetCache()){}

        public {{ entityNamePascalCase }}ContentModel(IUow uow, ICache cache)
        {
            _cache = cache;
            _uow = uow;
        }

        public I{{ entityNamePascalCase }}ContentModel Get()
        {
            var contentModel = new {{ entityNamePascalCase }}ContentModel(_uow, _cache);
            
            return contentModel;
        }

        protected readonly IUow _uow;
        protected readonly ICache _cache;
    }
}
