using {{ rootNamespacename }}.Data.Model;

namespace {{ namespacename }}
{
    public class {{ entityNamePascalCase }}ApiModel
    {        
        public int Id { get; set; }
        public int? TenantId { get; set; }
        public string Name { get; set; }

        public static TModel From{{ entityNamePascalCase }}<TModel>({{ entityNamePascalCase }} {{ entityNameCamelCase }}) where
            TModel : {{ entityNamePascalCase }}ApiModel, new()
        {
            var model = new TModel();
            model.Id = {{ entityNameCamelCase }}.Id;
            model.TenantId = {{ entityNameCamelCase }}.TenantId;
            model.Name = {{ entityNameCamelCase }}.Name;
            return model;
        }

        public static {{ entityNamePascalCase }}ApiModel From{{ entityNamePascalCase }}({{ entityNamePascalCase }} {{ entityNameCamelCase }})
            => From{{ entityNamePascalCase }}<{{ entityNamePascalCase }}ApiModel>({{ entityNameCamelCase }});

    }
}
